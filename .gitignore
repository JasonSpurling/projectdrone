# Secrets -- never commit. Real credentials live here (OpenSky client
# secret, DETECTIONS_API_KEY). Copy .env.example instead.
.env

# Build artifacts
build/

# Runtime data (SQLite DB, downloaded registry cache) -- generated at
# runtime into /data inside the container; only relevant here if someone
# points DB_PATH into the repo for local (non-Docker) runs.
*.db
*.db-journal
*.zip

# OS / editor cruft
.DS_Store
Thumbs.db
.vscode/
.idea/
